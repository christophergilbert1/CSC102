<!-- 
    Author: Christopher Gilbert
    Date: 6/14/2024
    Description: Multi page website
-->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Combined Web Application</title>
    <style>
        /* CSS for the body */
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            padding: 0;
        }

        /* CSs for the header and footer */
        header, footer {
            background-color: #333;
            color: white;
            padding: 10px 0;
            text-align: center;
        }

        /* CSS for the navigation menu */
        nav {
            background-color: #444;
            padding: 10px 0;
        }

        /* CSSfor the navigation links */
        nav a {
            color: white;
            text-decoration: none;
            padding: 8px 16px;
            display: inline-block;
        }

        /* Hover effect for navigation links */
        nav a:hover {
            background-color: #555;
        }

        /* Content box for page content */
        .container {
            padding: 20px;
        }

        /* Enhanced CSs for tables */
        table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
            font-size: 18px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.15);
        }

        /* CSS for table borders */
        table, th, td {
            border: 1px solid #ddd;
        }

        /* Padding and text alignment for table cells */
        th, td {
            padding: 12px;
            text-align: left;
        }

        /* Background color for table headers */
        th {
            background-color: #4CAF50;
            color: white;
        }

        /* Alternating row colors */
        tr:nth-child(even) {
            background-color: #f2f2f2;
        }

        /* Hover effect for table rows */
        tr:hover {
            background-color: #f1f1f1;
        }

        /* Styling for the game button */
        #gameButton {
            padding: 20px;
            font-size: 20px;
            background-color: #4bdb4f;
            color: white;
            border: none;
            border-radius: 10px;
            cursor: pointer;
        }

        /* Styling for the disabled game button */
        #gameButton:disabled {
            background-color: #888;
            cursor: not-allowed;
        }

        /* Styling for the game timer and score */
        #timer, #score {
            font-size: 24px;
            margin-top: 20px;
        }

        /* Initially hide all page sections */
        .page-section {
            display: none;
        }

        /* Styling for warning messages */
        .warning {
            color: red;
            font-weight: bold;
        }

        /* Styling for the secret message */
        #secretMessage {
            display: none;
            font-size: 1.5em;
            color: blue;
            margin-top: 20px;
        }

        /* Styling for meme images */
        .meme-container img {
            max-width: 100%;
            height: auto;
            margin-bottom: 10px;
        }
    </style>
</head>
<body>
    <!-- Header section containing the main title -->
    <header>
        <h1>5.1 Final project</h1>
    </header>
    
    <!-- Navigation menu -->
    <nav id="mainNav">
        <h2>Navigation</h2>
        <a href="javascript:showSection('boards')">Boards</a><br>
        <a href="javascript:showSection('game')">Game</a><br>
        <a href="javascript:showSection('meme')">Meme</a><br>
        <a href="javascript:showSection('palindrome')">Check Palindrome</a><br>
        <a href="javascript:showSection('user_input')">User Input</a><br>
        <a href="javascript:showSection('table')">Table</a><br>
        <div id="homeLinkContainer"></div>
    </nav>
    
    <!-- Home section content -->
    <div id="home" class="page-section container">
        <h2>Welcome to the Landing Page</h2>
        <p>This is the main landing page of our web application. This website contains several interactive features including a button-click game, a palindrome checker, a user input validation tool, and detailed information about robotics and embedded systems. Navigate through the different sections using the menu to explore each feature.</p>
    </div>

    <!-- Boards section content -->
    <div id="boards" class="page-section container">
        <h2>About Robotics and Embedded Systems</h2>
        <p>Robots and connected embedded digital systems provide smart solutions that extend human capabilities. Powered by artificial intelligence and real-time data generated by nature, people, and the systems within robots and drones, the degrees of autonomy surrounding us increase as they perform valued tasks.</p>
        <p>UAT’s Robotics and Embedded Systems (RES) degree provides students with the foundation for the design, implementation, and analysis of embedded systems that make robots, drones, and other platforms viable tools. The RES degree incorporates mechanical design, digital logic design, embedded programming, machine vision, sensing, big data, and artificial intelligence into the development and design of robotic systems that solve useful problems.</p>
    </div>

    <!-- Game section content -->
    <div id="game" class="page-section container">
        <h2>Click the Button Game</h2>
        <p>Click the button as many times as you can in 10 seconds!</p>
        <button id="gameButton" onclick="clickButton()" disabled>Click Me!</button>
        <div id="timer">Time: 10</div>
        <div id="score">Score: 0</div>
        <button onclick="startGame()">Start Game</button>
    </div>

    <!-- Meme section content -->
    <div id="meme" class="page-section container meme-container">
        <h2>Welcome to the Meme Page</h2>
        <p>Here are some funny memes:</p>
        <!-- meme images -->
        <img src="Pictures/download (1).jpg" alt="Meme 1">
        <img src="Pictures/download (2).jpg" alt="Meme 2">
        <img src="Pictures/download (3).jpg" alt="Meme 3">
        <img src="Pictures/download (4).jpg" alt="Meme 4">
        <img src="Pictures/download.jpg" alt="Meme 5">
    </div>

    <!-- Palindrome section -->
    <div id="palindrome" class="page-section container">
        <h2>Check if a String is a Palindrome</h2>
        <input type="text" id="userInput" placeholder="Enter a string">
        <button onclick="checkPalindrome()">Check</button>
        <div id="result"></div>
    </div>

    <!-- User input validation section -->
    <div id="user_input" class="page-section container">
        <h2>User Input Validation</h2>
        <input type="text" id="firstName" placeholder="Enter your first name">
        <input type="text" id="lastName" placeholder="Enter your last name">
        <input type="text" id="zipCode" placeholder="Enter your zip code">
        <button onclick="validateUserInput()">Validate</button>
        <div id="warning" class="warning"></div>
    </div>

    <!-- Table section content -->
    <div id="table" class="page-section container">
        <h2>Useful Information About Robotics</h2>
        <table>
            <tr>
                <th>Component</th>
                <th>Function</th>
                <th>Description</th>
            </tr>
            <tr>
                <td>Microcontroller</td>
                <td>Control</td>
                <td>A microcontroller is a compact integrated circuit designed to govern a specific operation in an embedded system. It is the brain of the robot, controlling all the other components.</td>
            </tr>
            <tr>
                <td>Servo Motor</td>
                <td>Movement</td>
                <td>Servo motors are used for precise control of angular or linear position, velocity, and acceleration. They are commonly used in robotics for accurate movements.</td>
            </tr>
            <tr>
                <td>Sensor</td>
                <td>Perception</td>
                <td>Sensors are devices that detect changes in the environment and send information to other electronics, frequently a computer processor. They are crucial for the robot to interact with its surroundings.</td>
            </tr>
            <tr>
                <td>Actuator</td>
                <td>Movement</td>
                <td>Actuators are components of a machine that are responsible for moving and controlling a mechanism or system. They convert energy (usually electrical) into physical motion.</td>
            </tr>
            <tr>
                <td>Power Supply</td>
                <td>Energy</td>
                <td>The power supply provides the necessary electrical power to all the components of the robot. It can be a battery or an external power source.</td>
            </tr>
        </table>
    </div>

    <!-- Footer with author and university information -->
    <footer>
        Christopher Gilbert<br>
        University of Advancing Technology<br>
        2625 W Baseline Rd<br>
        Tempe, AZ 85283<br>
    </footer>

    <script>
        // Shows the selected section and hide others
        function showSection(sectionId) {
            // Select all page sections
            const sections = document.querySelectorAll('.page-section');
            sections.forEach(section => {
                section.style.display = section.id === sectionId ? 'block' : 'none';
            });

            // Gets the info for the home link
            const homeLinkContainer = document.getElementById('homeLinkContainer');
            // If the current section is not home and the home link is not already present it will add the home button 
            if (sectionId !== 'home' && !homeLinkContainer.querySelector('a')) {
                const homeLink = document.createElement('a');
                homeLink.href = 'javascript:showSection(\'home\')';
                homeLink.textContent = 'Home';
                homeLinkContainer.appendChild(homeLink);
                homeLinkContainer.appendChild(document.createElement('br'));
            }
            // If the current section is home it will remove the home button
            if (sectionId === 'home') {
                homeLinkContainer.innerHTML = '';
            }
        }

        // Lwill show the landing (home) as first page
        showSection('home');

        // conditons for the game 
        let timeLeft = 10;
        let score = 0;
        let timerInterval;

        // Button to start the game
        function startGame() {
            // Reset game 
            timeLeft = 10;
            score = 0;
            document.getElementById('score').innerHTML = `Score: ${score}`;
            document.getElementById('timer').innerHTML = `Time: ${timeLeft}`;
            document.getElementById('gameButton').disabled = false;
            // Starts the countdown timer
            timerInterval = setInterval(countdown, 1000);
        }

        // handles the countdown timer
        function countdown() {
            timeLeft--;
            document.getElementById('timer').innerHTML = `Time: ${timeLeft}`;
            if (timeLeft <= 0) {
                // Stop the game when the timer reaches 0
                clearInterval(timerInterval);
                document.getElementById('gameButton').disabled = true;
                alert(`Game over! Your score is ${score}.`);
            }
        }

        // able to handle and count the button clicked for the game
        function clickButton() {
            score++;
            document.getElementById('score').innerHTML = `Score: ${score}`;
        }

        // checks if a string is a palindrome
        function checkPalindrome() {
            // Gets the user input
            var userInput = document.getElementById('userInput').value.trim().toLowerCase();
            // Reverse the string
            var reversedStr = userInput.split('').reverse().join('');
            // Checks if the input is a palindrome
            if (userInput === reversedStr) {
                document.getElementById('result').innerHTML = "It's a palindrome!";
            } else {
                document.getElementById('result').innerHTML = "It's not a palindrome.";
            }
        }

        // validates user input
        function validateUserInput() {
            // Get the user info
            var firstName = document.getElementById('firstName').value.trim();
            var lastName = document.getElementById('lastName').value.trim();
            var zipCode = document.getElementById('zipCode').value.trim();

            // Combine the first and last names
            var fullName = firstName + " " + lastName;

            // Validate the full name length
            if (fullName.length > 20) {
                document.getElementById('warning').innerHTML = "Full name cannot exceed 20 characters.";
                return;
            }

            // Validate the zip code format
            if (!/^\d{5}$/.test(zipCode)) {
                document.getElementById('warning').innerHTML = "Zip code must be 5 digits.";
                return;
            }

            // If validation passes, show a success message
            document.getElementById('warning').innerHTML = "Input is valid!";
        }
    </script>
</body>
</html>
